# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Segredos locais (chave service_role do Supabase etc.) — nunca commitar
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local config/skills (CLAUDE.md at repo root stays tracked)
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Material bruto de fotos/vídeos/PDFs de Letícia (1GB+, organizado localmente em
# public/assets/ como backup/staging — a hospedagem real já é o Supabase Storage, ver
# src/lib/ARCHITECTURE.md). Grande demais e desnecessário pro git.
/public/assets/
